from index import events
from data import Course

def filterEvents:
    for course in courses:
        course = querySelector.Course(name)
        userEvents = []
        if "{{course}}" in event.summary:
            userEvents.append(event)
    return userEvents
